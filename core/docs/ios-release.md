# iOS App Store Release Runbook

This document is the single source of truth for TinyCld's iOS App Store listing content, data-collection declarations, review notes, and versioning policy. Update it every time App Store Connect metadata changes.

## App Store Connect record

- **App Name:** TinyCld
- **Subtitle:** Self-hosted mail, calendar, contacts, drive
- **Bundle ID:** org.tinycld.app
- **Primary category:** Productivity
- **Secondary category:** Business
- **Age rating:** 4+
- **Copyright:** © 2026 Nathan Stitt
- **Marketing URL:** https://tinycld.org
- **Support URL:** https://tinycld.org/support
- **Privacy Policy URL:** https://tinycld.org/privacy

## Description

TinyCld is a self-hosted productivity suite that connects to a PocketBase server you control. Your mail, calendar, contacts, and files live on your own hardware — not ours.

- **Bring your own server.** Point the app at your TinyCld instance to get going. Don't have one yet? Use the built-in demo server to try before you install.
- **Mail.** Threaded conversations, labels, attachments, IMAP/SMTP.
- **Calendar.** Recurring events, guest RSVP, reminders, CalDAV.
- **Contacts.** Shared directory, favorites, CardDAV.
- **Drive.** Files, versioning, share links, role-based permissions, WebDAV.
- **Open source.** AGPL-3.0. Every line is auditable at github.com/tinycld/tinycld.

No analytics, no ad SDKs, no tracking pixels. Diagnostic crash reports are anonymized.

## Keywords (100 char limit, comma-separated)

email,calendar,contacts,drive,self-hosted,IMAP,CalDAV,CardDAV,WebDAV,privacy,open source

## Screenshot inventory

| Device class | Resolution | Screens |
| --- | --- | --- |
| 6.9" iPhone (iPhone 16 Pro Max) | 1320 × 2868 | 5 |
| 6.5" iPhone (iPhone 11 Pro Max) | 1242 × 2688 | 5 |
| 13" iPad Pro M4 | 2064 × 2752 | 5 |
| 12.9" iPad Pro (6th gen) | 2048 × 2732 | 5 |

The same five compositions are captured on every device: (1) mail thread view, (2) calendar week view, (3) contacts list with detail pane, (4) drive file browser, (5) settings with About section + dark mode visible.

## Data-collection questionnaire

- **Data collected:** Diagnostics → Crash Data, Other Diagnostic Data
- **Linked to user identity:** No
- **Used for tracking:** No
- **Purposes:** App Functionality, Analytics
- **Third parties:** Sentry (https://sentry.io)

## Review notes template

Copy this verbatim into App Store Connect → App Review Information → Notes when submitting. Fill in the demo password from 1Password (entry: "App Review demo password").

    Thanks for reviewing TinyCld.

    TinyCld is a self-hosted productivity suite: users normally connect the
    app to their own PocketBase server. For review, we provide a hosted demo
    server and a pre-seeded demo account:

        Demo server URL: https://tinycld.org
        Email:           appreview@tinycld.org
        Password:        <FILL IN FROM 1PASSWORD>

    On the first screen, tap "Use tinycld.org" to connect to the demo
    server, then sign in with the credentials above. The demo account is
    reseeded nightly, so Mail, Calendar, Contacts, and Drive are always
    populated.

    Account deletion: Settings → Personal → Delete my account. This
    anonymizes the user record and removes the user from every org.

    Privacy policy: https://tinycld.org/privacy
    Support: https://tinycld.org/support or support@tinycld.org

    Contact during review: nathan@stitt.org (replies within 24h).

## Version / build number policy

- **Marketing version** lives in `app.json` `expo.version`. Bump it for every App Store submission.
- **Build number** is auto-incremented by EAS (`production` profile, `autoIncrement: true`).
- **Never** hand-edit `CFBundleShortVersionString` or `CFBundleVersion` in `ios/TinyCld/Info.plist`. The `ios/` directory is gitignored and regenerated by every `expo prebuild`.

## EAS build profiles (see `eas.json`)

| Profile | Env | Purpose |
| --- | --- | --- |
| `development` | dev client | Local iteration |
| `preview` | internal distribution | Team testing |
| `review` | `EXPO_PUBLIC_APP_REVIEW_MODE=1` | Builds for App Review only — exposes "Fill demo credentials" button |
| `production` | auto-increment build | TestFlight + App Store |

**Never submit a `review` build to App Store Connect.** The review profile is for building an .ipa that behaves exactly like the submission build except that the sign-in screen offers a "Fill demo credentials" affordance. Use the `production` profile for the actual submission.

## Submission steps

1. Ensure `docs/ios-release.md` (this file) is up to date.
2. `pnpm run packages:generate` (picks up any new package wiring).
3. `pnpm run checks` — must pass.
4. `pnpm exec expo prebuild --platform ios --clean` — regenerates `ios/` from `app.json`.
5. `pnpm exec eas build --platform ios --profile production`.
6. Wait for TestFlight processing; install on a physical iPhone + iPad and run through the end-to-end checklist (see section below).
7. In App Store Connect, fill metadata from this document.
8. Attach screenshots (5 per device class).
9. Paste the review notes template from above, with the demo password filled in.
10. Submit for Review. Apple typically responds in 24–48h.
11. On approval, release manually (not automatic) so post-approval issues can be caught first.

## End-to-end verification checklist (TestFlight)

- Fresh install → lands on `/connect` without AuthGate flash.
- "Use tinycld.org" (primary CTA) → connects directly to the demo server.
- "I host my own server" → opens the URL sheet → Connect succeeds with a custom address.
- Sign in with demo account → primary org loads.
- Browse mail, calendar, contacts, drive — all populated.
- Toggle system dark mode → app follows.
- Settings → Personal → About shows version, commit, server.
- Settings → Personal → Disconnect server → returns to `/connect`, credentials cleared.
- Sign back in → Settings → Personal → Delete account → type email → confirm → returns to `/connect` and old email can no longer sign in.
- Force a test crash in a TestFlight build → Sentry receives the event with scrubbed PII.
- Push notification from server → delivered to the device with production APNs cert.

## Known rejection risks (document mitigations)

- **Self-hosted model misread as "incomplete."** Mitigation: review notes explicitly explain the server-URL step; default-server button eliminates friction.
- **Missing privacy manifest.** Mitigation: `ios/TinyCld/PrivacyInfo.xcprivacy` declares all required reason APIs; third-party SDKs (Sentry, Expo modules) ship their own manifests.
- **Encryption export compliance.** Already declared `ITSAppUsesNonExemptEncryption: false` in `app.json` — HTTPS only, no custom crypto.

## Assets

Store large binary assets for the listing (screenshots, app preview, marketing icon) in a separate location outside this repo (e.g. `~/Dropbox/tinycld/app-store/`). The repo tracks policy and configuration only.
